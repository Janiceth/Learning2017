#!/urs/bin/env python3

import sys

def MinToHour(min):
        h = min / 60
        m =min % 60
        print("{:.0f}{}, {:.2f}{}".format(h, "H", m, "M"))
# MinToHour(88)
try:
        MinToHour()
        if min
        raise ValueError("Enter value Error!")
        raise KeyboardInterrupt("KeyboardInterrupt)